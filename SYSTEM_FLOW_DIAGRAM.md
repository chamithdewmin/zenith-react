# Contact Form Data Flow

## 📊 System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERACTION                        │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     REACT FRONTEND (ContactUs.jsx)              │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  1. User fills form (name, email, phone, subject, msg)  │   │
│  │  2. Client-side validation (validateForm)               │   │
│  │  3. Submit button clicked                               │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                  API SERVICE (contactApi.js)                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  1. Validates email format                              │   │
│  │  2. Validates phone number (optional)                   │   │
│  │  3. Checks required fields                              │   │
│  │  4. Creates JSON payload                                │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
                   HTTP POST Request
                   (JSON via fetch API)
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│              HOSTINGER SERVER (contact.php)                     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  1. Receives POST request                               │   │
│  │  2. Handles CORS headers                                │   │
│  │  3. Validates request method                            │   │
│  │  4. Connects to MySQL database                          │   │
│  │  5. Sanitizes & validates input                         │   │
│  │  6. Prepares SQL statement (PDO)                        │   │
│  │  7. Executes INSERT query                               │   │
│  │  8. Sends email notification (optional)                 │   │
│  │  9. Returns JSON response                               │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                   MYSQL DATABASE (Hostinger)                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Table: contact_submissions                             │   │
│  │  ┌──────┬───────┬────────┬────────┬─────────┬─────────┐│   │
│  │  │  id  │ name  │ email  │ phone  │ subject │ message ││   │
│  │  ├──────┼───────┼────────┼────────┼─────────┼─────────┤│   │
│  │  │  1   │ John  │ j@...  │ +123.. │ Inquiry │ Hello...││   │
│  │  │  2   │ Jane  │ ja@... │ +456.. │ Support │ Need... ││   │
│  │  └──────┴───────┴────────┴────────┴─────────┴─────────┘│   │
│  │  + created_at, status columns                           │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    EMAIL NOTIFICATION (Optional)                │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  To: isuru.warnakula@zenithscs.com.au                   │   │
│  │  Subject: New Contact Form Submission                   │   │
│  │  Body: Name, Email, Phone, Subject, Message             │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      ADMIN PANEL (admin.php)                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  View all submissions in a table                        │   │
│  │  - Date, Name, Email, Phone, Subject, Message, Status   │   │
│  │  - Statistics (Total, New, Today)                       │   │
│  │  - Authentication required                              │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request/Response Flow

### Successful Submission

```
CLIENT                      SERVER                    DATABASE
  │                          │                          │
  │  POST /api/contact.php   │                          │
  ├─────────────────────────>│                          │
  │  { name, email, ... }    │                          │
  │                          │                          │
  │                          │  Connect to MySQL        │
  │                          ├─────────────────────────>│
  │                          │<─────────────────────────┤
  │                          │  Connection OK           │
  │                          │                          │
  │                          │  Validate & Sanitize     │
  │                          │                          │
  │                          │  INSERT INTO contact_... │
  │                          ├─────────────────────────>│
  │                          │<─────────────────────────┤
  │                          │  Insert Success (ID: 42) │
  │                          │                          │
  │                          │  Send Email (optional)   │
  │                          │                          │
  │  200 OK                  │                          │
  │<─────────────────────────┤                          │
  │  { success: true, ... }  │                          │
  │                          │                          │
  │  Show success message    │                          │
  │  Clear form              │                          │
```

### Failed Submission (Validation Error)

```
CLIENT                      SERVER
  │                          │
  │  POST /api/contact.php   │
  ├─────────────────────────>│
  │  { email: "invalid" }    │
  │                          │
  │                          │  Validate Email
  │                          │  ❌ Invalid format
  │                          │
  │  400 Bad Request         │
  │<─────────────────────────┤
  │  { success: false, ... } │
  │                          │
  │  Show error message      │
  │  Highlight field         │
```

---

## 📁 File Relationships

```
┌─────────────────────────────────────────────┐
│         React Application                   │
│                                             │
│  ContactUs.jsx                              │
│    ├── imports contactApi.js               │
│    ├── uses validateForm()                 │
│    └── calls submitContactForm()           │
│                                             │
│  contactApi.js                              │
│    ├── exports validateForm()              │
│    ├── exports submitContactForm()         │
│    └── uses VITE_API_URL from .env         │
│                                             │
│  .env                                       │
│    └── VITE_API_URL=https://domain/api/... │
└─────────────────────────────────────────────┘
                    │
                    │ HTTP Request
                    ▼
┌─────────────────────────────────────────────┐
│         Hostinger Server                    │
│                                             │
│  contact.php                                │
│    ├── receives POST request                │
│    ├── connects to MySQL                    │
│    ├── uses database credentials            │
│    └── returns JSON response                │
│                                             │
│  admin.php                                  │
│    ├── authentication                       │
│    ├── connects to MySQL                    │
│    └── displays submissions table           │
│                                             │
│  MySQL Database                             │
│    └── contact_submissions table            │
│         └── stores all form data            │
└─────────────────────────────────────────────┘
```

---

## 🔐 Security Flow

```
┌─────────────────────────────────────────────┐
│         Security Layers                     │
└─────────────────────────────────────────────┘

1. CLIENT-SIDE VALIDATION (React)
   ├── Email format check
   ├── Required fields check
   ├── Phone format validation
   └── Message length check

2. TRANSPORT SECURITY
   ├── HTTPS encryption (SSL/TLS)
   └── CORS policy enforcement

3. SERVER-SIDE VALIDATION (PHP)
   ├── Request method check (POST only)
   ├── Input sanitization (htmlspecialchars)
   ├── Email validation (filter_var)
   ├── SQL injection prevention (PDO prepared statements)
   └── Error handling & logging

4. DATABASE SECURITY
   ├── Limited user permissions
   ├── Parameterized queries (no SQL injection)
   ├── Indexed columns for performance
   └── Character set: UTF-8 (utf8mb4)

5. ADMIN PANEL SECURITY
   ├── Session-based authentication
   ├── Password protection
   └── Login required to view data
```

---

## 🎯 Component Interaction Map

```
┌──────────────────────────────────────────────────────────┐
│                                                          │
│  ContactUs.jsx (Main Component)                         │
│                                                          │
│  ┌────────────────────────────────────────────────┐     │
│  │  State Management                              │     │
│  │  - formData { name, email, phone, ... }        │     │
│  │  - notification { message, type }              │     │
│  │  - isSubmitting (boolean)                      │     │
│  │  - errors { name: '', email: '', ... }         │     │
│  └────────────────────────────────────────────────┘     │
│                                                          │
│  ┌────────────────────────────────────────────────┐     │
│  │  Event Handlers                                │     │
│  │  - handleChange() → updates formData           │     │
│  │  - handleSubmit() → validates & submits        │     │
│  └────────────────────────────────────────────────┘     │
│                                                          │
│  ┌────────────────────────────────────────────────┐     │
│  │  UI Components                                 │     │
│  │  - Header, ScrollHeader                        │     │
│  │  - HeroSection                                 │     │
│  │  - Contact Info Section                        │     │
│  │  - Contact Form                                │     │
│  │  - Toast Notification                          │     │
│  │  - Footer, ScrollToTopButton                   │     │
│  └────────────────────────────────────────────────┘     │
│                                                          │
└──────────────────────────────────────────────────────────┘
                        │
                        │ imports
                        ▼
┌──────────────────────────────────────────────────────────┐
│  contactApi.js (Service Layer)                          │
│                                                          │
│  ┌────────────────────────────────────────────────┐     │
│  │  Functions Exported:                           │     │
│  │  1. submitContactForm(formData)                │     │
│  │     └── Sends data to API endpoint             │     │
│  │  2. validateForm(formData)                     │     │
│  │     └── Returns { isValid, errors }            │     │
│  │  3. validateEmail(email)                       │     │
│  │  4. validatePhone(phone)                       │     │
│  └────────────────────────────────────────────────┘     │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

---

## 📊 Data Transformation Journey

```
1. USER INPUT
   { 
     name: "John Doe",
     email: "john@example.com",
     phone: "+61 433 881 247",
     subject: "Business Inquiry",
     message: "I need help with VRP planning..."
   }
   
   ▼ handleChange() updates state

2. REACT STATE
   formData = {
     name: "John Doe",
     email: "john@example.com",
     phone: "+61 433 881 247",
     subject: "Business Inquiry",
     message: "I need help with VRP planning..."
   }
   
   ▼ handleSubmit() validates

3. VALIDATION
   validation = validateForm(formData)
   {
     isValid: true,
     errors: {}
   }
   
   ▼ submitContactForm() sends

4. API REQUEST
   POST https://yourdomain.com/api/contact.php
   Content-Type: application/json
   Body: JSON.stringify(formData)
   
   ▼ Server receives

5. PHP PROCESSING
   $data = json_decode(file_get_contents('php://input'));
   $name = htmlspecialchars(trim($data['name']));
   // ... sanitize all fields
   
   ▼ Database insert

6. DATABASE RECORD
   INSERT INTO contact_submissions VALUES (
     NULL,                           -- id (auto-increment)
     'John Doe',                     -- name
     'john@example.com',             -- email
     '+61 433 881 247',              -- phone
     'Business Inquiry',             -- subject
     'I need help with VRP...',      -- message
     NOW(),                          -- created_at
     'new'                           -- status
   )
   
   ▼ Response sent

7. API RESPONSE
   {
     "success": true,
     "message": "Message sent successfully!"
   }
   
   ▼ React updates UI

8. USER FEEDBACK
   ✅ Success notification displayed
   📝 Form cleared and ready for next submission
```

---

This visualization helps understand how data flows through the entire system from user input to database storage!
